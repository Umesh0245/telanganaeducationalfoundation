---
import MainLayout from '../layouts/MainLayout.astro';
import { impactStats, programmes, campaigns, accreditations, foundationGallery } from '../data/site';
---

<MainLayout
  title="Telangana Educational Foundation"
  description="Telangana Educational Foundation advances inclusive development through education, healthcare, women empowerment and livelihood."
  activePath="/"
>
  <section class="section landing-intro" data-reveal>
    <div class="landing-intro-topbar">
      <p class="landing-intro-title">
        <span>Merit Scholarship Program</span>
        <strong>Admissions Window Open</strong>
      </p>
      <div class="landing-intro-actions">
        <a class="intro-donate" href="/donate">Donate Now</a>
        <span class="intro-menu" aria-hidden="true">☰</span>
      </div>
    </div>

    <article class="landing-intro-hero">
      <div class="landing-intro-quote">
        <span class="intro-quote-kicker">Merit Scholarship 2026</span>
        <h1>Bringing Smiles Through Education & Empowerment</h1>
        <div class="intro-quote-body">
          <p>
            Telangana Educational Foundation empowers students from Telangana with merit-based scholarship access,
            transparent selection and family-focused academic continuity support.
          </p>
          <p class="intro-quote-highlight">
            1,000 qualified students can receive scholarship support up to Rs 4,50,000 for higher education pathways.
          </p>
          <div class="intro-steps" aria-label="Scholarship steps quick view">
            <span><strong>1</strong>Offline Test In College</span>
            <span><strong>2</strong>Online Registration (Rs 900)</span>
            <span><strong>3</strong>Online Test + Interview</span>
            <span><strong>4</strong>Top 1000: Up To Rs 4,50,000</span>
          </div>
          <ul class="intro-quote-points">
            <li>Clear process from offline college test to online interview.</li>
            <li>Merit, documentation and eligibility based final selection.</li>
            <li>Support aimed at real progression into professional courses.</li>
          </ul>
        </div>
        <div class="landing-intro-cta-row">
          <a class="intro-btn intro-btn-secondary" href="/programmes">Know More</a>
          <a class="intro-btn intro-btn-primary" href="/donate">Support A Student</a>
        </div>
      </div>

      <div class="landing-intro-image-wrap">
        <div class="hero-image-bg">
          <img
            class="landing-intro-image hero-image-cover"
            src="/img.webp"
            alt="Group of Indian college students, smiling, with backpacks, on transparent background"
            loading="eager"
            decoding="async"
          />
          <div class="hero-image-headline">
            <span class="hero-image-kicker">Merit Scholarship 2026</span>
            <h2>Scholarships For<br />Bright Futures</h2>
            <a class="hero-image-cta" href="/programmes">Know More &rsaquo;</a>
          </div>
        </div>
      </div>
    </article>
  </section>

  <section class="section landing-scholarship" data-reveal>
    <div class="landing-scholarship-top">
      <article class="card landing-scholarship-copy" data-pulse>
        <span class="scholarship-chip">Merit Scholarship Program 2026</span>
        <h1>Empowering Telangana Students Through Higher-Education Scholarships</h1>
        <p class="lead">
          TEF scholarship initiative supports deserving intermediate passing students with structured financial
          assistance, transparent selection and academic continuity support.
        </p>
        <p class="hero-scholarship-note">
          For <strong>1,000 students</strong>, scholarship support is available up to <strong>Rs 4,50,000</strong>
          based on merit and eligibility.
        </p>
        <div class="pill-row">
          <a href="/donate">Support This Scholarship</a>
          <a href="/programmes">Eligibility & Programmes</a>
        </div>
      </article>

      <article class="card landing-flyer-card" data-pulse>
        <img
          class="landing-flyer"
          src="https://upload.wikimedia.org/wikipedia/commons/8/8d/Jyothi_engineering_college_vettikkattiri_students.jpg"
          alt="Indian students preparing for engineering education"
          loading="eager"
        />
        <p class="landing-image-note">Indian students preparing for engineering and technical higher education.</p>
      </article>
    </div>

    <article class="card landing-details" data-pulse>
      <h3>Scholarship Details At A Glance</h3>
      <p>
        The scholarship flow is presented as a fixed four-step process for students and families.
      </p>
      <div class="scholarship-study-grid" aria-label="Students studying through scholarship support">
        <article class="study-image-card">
          <img
            src="https://upload.wikimedia.org/wikipedia/commons/8/8e/Students_at_SRMS_CET_Unnao.jpg"
            alt="Indian students in a college learning environment"
            loading="lazy"
          />
          <p>Intermediate to engineering transition support</p>
        </article>
        <article class="study-image-card">
          <img
            src="https://upload.wikimedia.org/wikipedia/commons/9/9b/PCoE_students_at_AICTE_Regional_Office_in_Mumbai.jpg"
            alt="Indian students engaging in technical education orientation"
            loading="lazy"
          />
          <p>Career and engineering guidance exposure</p>
        </article>
        <article class="study-image-card">
          <img
            src="https://upload.wikimedia.org/wikipedia/commons/c/c3/M._Venkaiah_Naidu_interacting_with_the_Students_after_inaugurating_the_newly_constructed_Lecture_Hall-cum-Laboratory_Complex%2C_at_the_Andhra_University_College_of_Engineering_for_Women%2C_in_Visakhapatnam%2C_Andhra_Pradesh.jpg"
            alt="Indian women engineering students in a higher education setting"
            loading="lazy"
          />
          <p>Women students in engineering pathways</p>
        </article>
      </div>
      <div class="landing-stat-row">
        <article class="landing-stat-card">
          <p class="stat-kicker">Total Intake</p>
          <h3>1,000 Students</h3>
          <p>Selected through fair screening and documented eligibility checks.</p>
        </article>
        <article class="landing-stat-card">
          <p class="stat-kicker">Scholarship Value</p>
          <h3>Up To Rs 4,50,000</h3>
          <p>Financial support for eligible intermediate passing candidates.</p>
        </article>
        <article class="landing-stat-card">
          <p class="stat-kicker">Required Documents</p>
          <h3>ID + Academics + Admission</h3>
          <p>ID proof, academic records and college admission proof are required.</p>
        </article>
      </div>
      <div class="scholarship-process" aria-label="Scholarship process steps">
        <span><strong>1</strong>Take Offline Test In Your College</span>
        <span><strong>2</strong>Qualified Students Register For Online Test (Rs 900)</span>
        <span><strong>3</strong>Take Online Test And Online Interview</span>
        <span><strong>4</strong>1,000 Qualified Students Can Get Rs 4,50,000 Scholarship</span>
      </div>
    </article>
  </section>

  <section class="section hero" data-reveal>
    <article class="card hero-copy" data-pulse>
      <p class="hero-kicker">Established For Lasting Change</p>
      <h1>Building Telangana's next generation through education, health and dignity.</h1>
      <p>
        Inspired by successful social development models, Telangana Educational Foundation creates integrated, measurable
        and community-driven change through learning, preventive healthcare, women empowerment and youth livelihood
        interventions.
      </p>
      <p class="hero-scholarship-note">
        Merit Scholarship 2026: Support for <strong>1,000 students</strong> with scholarship assistance up to
        <strong>Rs 4,50,000</strong> for eligible intermediate passing students.
      </p>
      <div class="pill-row">
        <a href="/donate">Donate Now</a>
        <a href="/programmes">Explore Programmes</a>
      </div>
    </article>

    <article class="card hero-visual" data-pulse>
      <!-- Main foundation image -->
      <div class="hero-mosaic-main">
        <img
          src="https://upload.wikimedia.org/wikipedia/commons/0/07/Teacher_explaining_students_in_Greenwood_High_School.jpg"
          alt="Teacher with students in a Telangana classroom learning session"
          loading="eager"
          class="hero-mosaic-img"
        />
        <div class="hero-mosaic-overlay"></div>
        <!-- Logo badge -->
        <div class="hero-logo-badge">
          <img src="/tef-logo-tight.png" alt="Telangana Educational Foundation" class="hero-logo-small" />
        </div>
        <p class="hero-mosaic-tag">Transforming Lives Across Telangana</p>
      </div>

      <!-- Bottom row: two supporting images + centre stat -->
      <div class="hero-mosaic-row">
        <div class="hero-mosaic-thumb">
          <img
            src="https://upload.wikimedia.org/wikipedia/commons/f/f3/Playing_students_yellanki_high_school.jpg"
            alt="Children playing and learning at Yellanki high school"
            loading="lazy"
          />
        </div>
        <div class="hero-mosaic-stat">
          <span class="hero-stat-num">12,000+</span>
          <span class="hero-stat-label">Lives Impacted</span>
        </div>
        <div class="hero-mosaic-thumb">
          <img
            src="https://upload.wikimedia.org/wikipedia/commons/3/3c/Childrens_day_group_photo.jpg"
            alt="Children group photo from a Telangana school event"
            loading="lazy"
          />
        </div>
      </div>

      <div class="hero-float">
        <h3>Impact Lens</h3>
        <p>Transparent action and district-level accountability.</p>
      </div>
    </article>
  </section>

  <section class="section" data-reveal>
    <h2>Our Impact</h2>
    <div class="grid">
      {
        impactStats.map((item) => (
          <article class="card" data-pulse>
            <h3>{item.value}</h3>
            <p>{item.label}</p>
          </article>
        ))
      }
    </div>
  </section>

  <section class="section" data-reveal>
    <h2>Our Programmes</h2>
    <div class="grid">
      {
        programmes.map((program, idx) => (
          <article class="card" data-pulse>
            <span class="badge">{`0${idx + 1}`}</span>
            <h3>{program.title}</h3>
            <p>{program.summary}</p>
          </article>
        ))
      }
    </div>
  </section>

  <section class="section" data-reveal>
    <h2>Field Moments</h2>
    <div class="image-grid">
      {
        foundationGallery.map((item) => (
          <article class="image-card" data-pulse>
            <img src={item.src} alt={item.alt} loading="lazy" decoding="async" />
            <div class="image-caption">
              <h3>{item.title}</h3>
              <p>{item.caption}</p>
            </div>
          </article>
        ))
      }
    </div>
  </section>

  <section class="section" data-reveal>
    <h2>Support A Cause</h2>
    <div class="grid">
      {
        campaigns.map((campaign, idx) => (
          <article class="card" data-pulse>
            <span class="badge">{`0${idx + 1}`}</span>
            <h3>{campaign.name}</h3>
            <p>{campaign.copy}</p>
            <div class="pill-row">
              <a href="/donate">Know More</a>
            </div>
          </article>
        ))
      }
    </div>
  </section>

  <section class="section" data-reveal>
    <h2>Empanelment And Accreditations</h2>
    <div class="grid">
      {
        accreditations.map((item) => (
          <article class="card" data-pulse>
            <p>{item}</p>
          </article>
        ))
      }
    </div>
  </section>
</MainLayout>

<style>
  .landing-intro {
    display: grid;
    gap: clamp(0.45rem, 1vw, 0.8rem);
    padding: clamp(0.35rem, 0.8vw, 0.6rem);
    background: rgba(255, 255, 255, 0.9);
  }

  .landing-intro-topbar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.8rem;
    padding: 0.45rem 0.6rem;
    border-radius: 14px;
    background: #ffffff;
    border: 1px solid rgba(120, 196, 150, 0.34);
  }

  .landing-intro-title {
    margin: 0;
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.42rem;
    line-height: 1;
  }

  .landing-intro-title span {
    width: fit-content;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    color: #16435f;
    font-weight: 700;
    font-size: 0.78rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    border: 1px solid rgba(92, 171, 121, 0.34);
    border-radius: 999px;
    padding: 0.23rem 0.58rem;
    background: rgba(227, 248, 234, 0.86);
    white-space: nowrap;
  }

  .landing-intro-title strong {
    display: inline-flex;
    align-items: center;
    color: #2f6a4c;
    font-size: 0.73rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-weight: 700;
    white-space: nowrap;
    padding-left: 0.48rem;
    border-left: 1px solid rgba(109, 183, 135, 0.48);
  }

  .landing-intro-actions {
    display: flex;
    align-items: center;
    gap: 0.55rem;
    flex-shrink: 0;
  }

  .intro-donate {
    border-radius: 10px;
    padding: 0.52rem 0.76rem;
    font-size: 0.82rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    color: #ffffff;
    background: linear-gradient(135deg, #5bb041, #4a9537);
  }

  .intro-menu {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 34px;
    height: 34px;
    border-radius: 8px;
    color: #193e59;
    font-size: 1.35rem;
    font-weight: 700;
    border: 1px solid rgba(115, 173, 207, 0.42);
    background: #ffffff;
  }

  .landing-intro-hero {
    min-height: clamp(418px, calc(100vh - 332px), 500px);
    display: grid;
    grid-template-columns: minmax(360px, 1.08fr) minmax(300px, 0.92fr);
    align-items: stretch;
    gap: 0;
    border-radius: 18px;
    border: 1px solid rgba(84, 160, 104, 0.3);
    background: linear-gradient(150deg, rgba(223, 246, 232, 0.7), rgba(205, 236, 220, 0.68));
    padding: clamp(0.36rem, 0.7vw, 0.52rem);
    column-gap: clamp(0.28rem, 0.55vw, 0.42rem);
  }

  .landing-intro-quote {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    justify-content: flex-start;
    gap: 0.34rem;
    padding: clamp(0.76rem, 1vw, 0.96rem);
    border-radius: 16px;
    background: linear-gradient(165deg, rgba(245, 255, 249, 0.99), rgba(215, 241, 223, 0.98));
    border: 1px solid rgba(65, 146, 95, 0.48);
    height: 100%;
    min-height: 100%;
    overflow: hidden;
    box-shadow: 0 16px 32px rgba(24, 96, 66, 0.16);
  }

  .intro-quote-kicker {
    margin: 0;
    width: fit-content;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.35rem 0.72rem;
    border-radius: 999px;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-size: 0.73rem;
    color: #145339;
    background: rgba(198, 240, 212, 0.96);
    border: 1px solid rgba(72, 158, 103, 0.46);
    font-weight: 700;
    line-height: 1;
    white-space: nowrap;
  }

  .landing-intro-quote h1 {
    margin: 0;
    color: #0b3049;
    max-width: 12ch;
    font-size: clamp(2.05rem, 3.25vw, 2.9rem);
    line-height: 0.92;
    letter-spacing: -0.02em;
  }

  .intro-quote-body {
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    gap: 0.48rem;
    width: 100%;
    flex: 1;
    margin-top: 0.72rem;
    padding-top: 0.4rem;
    border-top: 1px solid rgba(76, 149, 102, 0.22);
  }

  .landing-intro-quote p {
    margin: 0;
    color: #183f56;
    max-width: 52ch;
    font-size: 0.93rem;
    line-height: 1.42;
  }

  .intro-quote-highlight {
    border-left: 4px solid #2f965f;
    background: rgba(185, 233, 201, 0.98);
    border-radius: 10px;
    padding: 0.5rem 0.66rem;
    color: #12442c;
    font-weight: 600;
    line-height: 1.38;
  }

  .intro-steps {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.46rem;
  }

  .intro-steps span {
    border: 1.5px solid rgba(43, 138, 88, 0.52);
    border-radius: 13px;
    padding: 0.55rem 0.62rem;
    background: linear-gradient(140deg, #ffffff 0%, rgba(212, 248, 228, 0.98) 100%);
    color: #0c3d22;
    font-size: 0.78rem;
    font-weight: 700;
    display: flex;
    align-items: center;
    gap: 0.48rem;
    line-height: 1.22;
    box-shadow: 0 3px 10px rgba(20, 100, 60, 0.12), 0 1px 3px rgba(20, 100, 60, 0.08);
  }

  .intro-steps strong {
    min-width: 1.5rem;
    height: 1.5rem;
    border-radius: 999px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    color: #ffffff;
    background: linear-gradient(135deg, #1fa05a, #136636);
    font-size: 0.76rem;
    font-weight: 800;
    flex-shrink: 0;
    box-shadow: 0 2px 8px rgba(16, 90, 50, 0.35);
  }

  .intro-quote-points {
    margin: 0;
    padding-left: 1.05rem;
    color: #173f55;
    display: grid;
    gap: 0.14rem;
    line-height: 1.32;
    font-size: 0.84rem;
  }

  .landing-intro-cta-row {
    display: flex;
    flex-wrap: wrap;
    gap: 0.46rem;
    margin-top: auto;
    padding-top: 0.3rem;
    align-items: center;
  }

  .intro-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: auto;
    min-width: 138px;
    max-width: max-content;
    height: auto;
    min-height: 44px;
    aspect-ratio: auto;
    border-radius: 14px;
    padding: 0.6rem 1rem;
    font-weight: 700;
    font-size: 0.88rem;
    line-height: 1;
    white-space: nowrap;
    text-align: center;
    transition: transform 0.18s ease, box-shadow 0.18s ease, background 0.18s ease, border-color 0.18s ease;
  }

  .intro-btn:hover {
    transform: translateY(-1px);
  }

  .intro-btn-secondary {
    color: #113f5d;
    background: linear-gradient(145deg, #ffffff, #effaf3);
    border: 1px solid rgba(82, 170, 114, 0.46);
    box-shadow: 0 7px 14px rgba(31, 112, 75, 0.14);
  }

  .intro-btn-primary {
    color: #ffffff;
    background: linear-gradient(135deg, #37ab70, #1f7b4d);
    border: 1px solid rgba(30, 118, 75, 0.55);
    box-shadow: 0 10px 18px rgba(30, 108, 72, 0.26);
  }

  .intro-btn-secondary:hover {
    background: linear-gradient(145deg, #ffffff, #e6f7ee);
    border-color: rgba(73, 162, 106, 0.58);
  }

  .intro-btn-primary:hover {
    background: linear-gradient(135deg, #3ab477, #216d48);
  }

  .landing-intro-image-wrap {
    position: relative;
    width: 100%;
    justify-self: stretch;
    height: 100%;
    min-height: 100%;
    overflow: hidden;
    background: linear-gradient(155deg, #0d324a 0%, #0f513d 52%, #166a46 100%);
    border-radius: 20px;
    border: 1.5px solid rgba(141, 223, 99, 0.3);
    box-shadow:
      0 0 0 1px rgba(255, 255, 255, 0.06),
      0 24px 48px rgba(12, 56, 40, 0.32),
      0 8px 16px rgba(0, 0, 0, 0.2);
  }

  .hero-image-bg {
    position: absolute;
    inset: 0;
    border-radius: inherit;
    overflow: hidden;
    isolation: isolate;
    background: linear-gradient(155deg, #0d324a 0%, #0f513d 52%, #166a46 100%);
  }

  .hero-image-bg::before {
    content: "";
    position: absolute;
    inset: -18%;
    z-index: 0;
    background:
      radial-gradient(circle at 20% 10%, rgba(130, 225, 165, 0.22), transparent 50%),
      radial-gradient(circle at 86% 20%, rgba(68, 164, 228, 0.16), transparent 46%),
      radial-gradient(circle at 55% 88%, rgba(21, 110, 73, 0.34), transparent 60%);
  }

  .hero-image-bg::after {
    content: "";
    position: absolute;
    inset: 0;
    z-index: 1;
    background: linear-gradient(to bottom, rgba(6, 28, 40, 0.34), rgba(6, 28, 40, 0.08) 34%, rgba(6, 28, 40, 0.58));
  }

  .landing-intro-image {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    display: block;
    z-index: 1;
    transition: transform 0.6s ease, filter 0.6s ease;
  }

  .hero-image-cover {
    object-fit: cover;
    object-position: center 56%;
    transform: scale(1.08);
    filter: saturate(1.08) contrast(1.05);
  }

  .landing-intro-image-wrap:hover .landing-intro-image {
    transform: scale(1.11);
  }

  .hero-image-headline {
    position: absolute;
    left: 1rem;
    right: auto;
    bottom: 1rem;
    z-index: 2;
    display: grid;
    gap: 0.62rem;
    align-content: end;
    padding: 1rem;
    max-width: min(84%, 420px);
    border-radius: 16px;
    background: linear-gradient(to top, rgba(8, 26, 36, 0.92), rgba(8, 26, 36, 0.52));
    backdrop-filter: blur(2px);
  }

  .hero-image-kicker {
    width: fit-content;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    color: #08293c;
    background: rgba(150, 231, 109, 0.95);
    border-radius: 999px;
    font-size: 0.7rem;
    letter-spacing: 0.11em;
    text-transform: uppercase;
    font-weight: 800;
    padding: 0.34rem 0.66rem;
  }

  .hero-image-headline h2 {
    margin: 0;
    color: #ffffff !important;
    font-size: clamp(2.05rem, 3.25vw, 2.95rem);
    font-weight: 900;
    line-height: 0.98;
    letter-spacing: -0.01em;
    text-transform: uppercase;
    text-shadow: 0 6px 24px rgba(0, 0, 0, 0.45);
  }

  .hero-image-cta {
    width: max-content;
    margin: 0;
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    color: #ffffff;
    font-size: 0.8rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    font-weight: 800;
    background: rgba(141, 223, 99, 0.22);
    border: 1px solid rgba(141, 223, 99, 0.56);
    border-radius: 999px;
    padding: 0.42rem 0.86rem;
    backdrop-filter: blur(6px);
    transition: background 0.2s ease, border-color 0.2s ease;
  }

  .hero-image-cta:hover {
    background: rgba(141, 223, 99, 0.36);
    border-color: rgba(141, 223, 99, 0.75);
    color: #ffffff;
  }

  .hero {
    display: grid;
    grid-template-columns: 1.05fr 1fr;
    gap: 0.9rem;
    min-height: 72vh;
  }

  .hero-copy {
    display: grid;
    align-content: center;
    gap: 1rem;
  }

  .hero-kicker {
    font-size: 0.78rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: #0f6e9b;
    font-weight: 600;
  }

  .hero-scholarship-note {
    border-left: 4px solid #43a067;
    background: rgba(225, 246, 232, 0.7);
    border-radius: 10px;
    padding: 0.7rem 0.8rem;
    color: #1f4c35;
    font-weight: 500;
  }

  .hero h1 {
    line-height: 1;
  }

  .hero-visual {
    min-height: 480px;
    position: relative;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    padding: 0;
    gap: 0;
  }

  /* ── Main image ───────────────────────────────────── */
  .hero-mosaic-main {
    flex: 1.5;
    position: relative;
    overflow: hidden;
    min-height: 0;
  }

  .hero-mosaic-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    border-radius: 0;
  }

  .hero-mosaic-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to bottom, rgba(8, 30, 50, 0.08) 30%, rgba(8, 30, 50, 0.55) 100%);
    pointer-events: none;
  }

  /* TEF logo badge, top-left */
  .hero-logo-badge {
    position: absolute;
    top: 0.9rem;
    left: 0.9rem;
    z-index: 3;
    padding: 0;
    border-radius: 999px;
    background: transparent;
    border: none;
    box-shadow: 0 8px 22px rgba(11, 56, 82, 0.32);
    overflow: hidden;
  }

  .hero-logo-small {
    display: block;
    width: 58px;
    height: 58px;
    object-fit: cover;
    object-position: center;
    border-radius: 50%;
  }

  .landing-scholarship {
    display: grid;
    gap: 1rem;
    align-content: start;
  }

  .landing-scholarship-top {
    display: grid;
    grid-template-columns: 1.03fr 0.97fr;
    gap: 1rem;
    align-items: stretch;
  }

  .landing-flyer-card,
  .landing-scholarship-copy {
    display: grid;
    gap: 0.9rem;
    align-content: start;
  }

  .landing-flyer-card {
    padding: 0.9rem;
  }

  .landing-flyer {
    width: 100%;
    height: 100%;
    max-height: 780px;
    object-fit: cover;
    border-radius: 16px;
    border: 1px solid rgba(84, 173, 113, 0.35);
    display: block;
  }

  .landing-image-note {
    margin: 0;
    font-size: 0.9rem;
    color: #2b6048;
    line-height: 1.5;
    font-weight: 600;
  }

  .scholarship-chip {
    display: inline-flex;
    width: fit-content;
    max-width: 100%;
    padding: 0.38rem 0.78rem;
    border-radius: 999px;
    font-size: 0.76rem;
    line-height: 1;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: #1f5e40;
    background: rgba(206, 242, 216, 0.9);
    border: 1px solid rgba(74, 171, 108, 0.4);
    font-weight: 700;
  }

  .landing-scholarship-copy h1 {
    margin: 0;
    font-size: clamp(2rem, 2.8vw, 2.5rem);
    line-height: 1.02;
  }

  .landing-details {
    display: grid;
    gap: 0.85rem;
  }

  .landing-details h3 {
    font-size: clamp(1.55rem, 2.2vw, 2rem);
  }

  .scholarship-study-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0.65rem;
  }

  .study-image-card {
    border: 1px solid rgba(101, 182, 128, 0.34);
    border-radius: 14px;
    background: rgba(255, 255, 255, 0.88);
    overflow: hidden;
  }

  .study-image-card img {
    width: 100%;
    aspect-ratio: 4 / 3;
    object-fit: cover;
    display: block;
  }

  .study-image-card p {
    margin: 0;
    padding: 0.5rem 0.6rem;
    font-size: 0.85rem;
    color: #24563f;
    font-weight: 600;
  }

  .landing-stat-row {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0.65rem;
  }

  .landing-stat-card {
    border-radius: 14px;
    border: 1px solid rgba(96, 182, 125, 0.34);
    background: rgba(238, 251, 243, 0.85);
    padding: 0.7rem 0.8rem;
  }

  .landing-stat-card h3 {
    margin: 0.2rem 0 0;
    font-size: clamp(1.2rem, 2vw, 1.5rem);
    color: #165b40;
  }

  .landing-stat-card p {
    margin-top: 0.28rem;
    color: #2f5f47;
    font-size: 0.92rem;
    line-height: 1.5;
  }

  .stat-kicker {
    margin: 0;
    text-transform: uppercase;
    letter-spacing: 0.07em;
    font-size: 0.7rem;
    color: #2d7a56;
    font-weight: 700;
  }

  .scholarship-process {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 0.55rem;
  }

  .scholarship-process span {
    border: 1px solid rgba(102, 186, 130, 0.35);
    border-radius: 12px;
    padding: 0.55rem 0.5rem;
    background: rgba(236, 250, 240, 0.95);
    color: #1f5e40;
    font-size: 0.8rem;
    font-weight: 700;
    display: flex;
    align-items: center;
    gap: 0.35rem;
    justify-content: center;
    text-align: center;
  }

  .scholarship-process strong {
    width: 1.2rem;
    height: 1.2rem;
    border-radius: 999px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    color: #ffffff;
    background: #2a8f60;
    font-size: 0.72rem;
    line-height: 1;
  }

  /* Tagline on main image */
  .hero-mosaic-tag {
    position: absolute;
    bottom: 0.85rem;
    left: 0.9rem;
    z-index: 3;
    margin: 0;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: #ffffff;
    text-shadow: 0 1px 4px rgba(0, 0, 0, 0.4);
  }

  /* ── Bottom row ───────────────────────────────────── */
  .hero-mosaic-row {
    flex: 1;
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    gap: 3px;
    background: #0d4a6b;
  }

  .hero-mosaic-thumb {
    overflow: hidden;
  }

  .hero-mosaic-thumb img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    border-radius: 0;
    transition: transform 0.5s ease;
  }

  .hero-mosaic-thumb:hover img {
    transform: scale(1.06);
  }

  /* Centre stat block */
  .hero-mosaic-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 0.7rem 1.1rem;
    background: linear-gradient(145deg, #0f6e9b, #0a4270);
    color: #ffffff;
    min-width: 108px;
    gap: 0.15rem;
  }

  .hero-stat-num {
    font-size: 1.65rem;
    font-weight: 800;
    font-family: 'Barlow Condensed', sans-serif;
    line-height: 1;
    letter-spacing: -0.01em;
  }

  .hero-stat-label {
    font-size: 0.65rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    opacity: 0.85;
    text-align: center;
  }

  .hero-float {
    position: absolute;
    right: 1rem;
    bottom: 1rem;
    max-width: 220px;
    padding: 0.8rem;
    border-radius: 14px;
    background: rgba(255, 255, 255, 0.82);
    border: 1px solid rgba(117, 190, 236, 0.35);
  }

  .hero-float h3,
  .hero-float p {
    margin: 0;
  }

  .hero-float p {
    margin-top: 0.35rem;
    color: var(--text-muted);
    line-height: 1.45;
  }

  @media (max-width: 1366px) {
    .hero {
      min-height: 66vh;
    }

    .landing-flyer {
      max-height: 710px;
    }

    .landing-stat-row {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .scholarship-study-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .landing-scholarship-copy h1 {
      font-size: clamp(1.9rem, 2.45vw, 2.25rem);
    }
  }

  @media (max-width: 1024px) {
    .landing-intro-hero {
      grid-template-columns: minmax(280px, 1fr) minmax(280px, 1fr);
      min-height: auto;
      gap: 0.6rem;
      padding: 0.55rem;
    }

    .landing-intro-quote {
      border-bottom: none;
      overflow: visible;
      display: flex;
    }

    .landing-intro-image-wrap {
      width: 100%;
      justify-self: stretch;
      aspect-ratio: 4 / 3;
      min-height: 340px;
      border-radius: 16px;
    }

    .hero {
      grid-template-columns: 1fr;
      min-height: auto;
    }

    .hero-visual,
    [data-hero-canvas] {
      min-height: 420px;
    }

    .landing-scholarship-top {
      grid-template-columns: 1fr;
    }

    .landing-scholarship-copy h1 {
      font-size: clamp(1.75rem, 5.2vw, 2.2rem);
    }

    .landing-flyer {
      max-height: 620px;
    }

    .scholarship-process {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .landing-stat-row {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 640px) {
    .landing-intro {
      padding: 0.55rem;
    }

    .landing-intro-topbar {
      padding: 0.45rem 0.5rem;
    }

    .landing-intro-title {
      gap: 0.3rem;
    }

    .landing-intro-title span {
      font-size: 0.67rem;
      padding: 0.2rem 0.44rem;
    }

    .landing-intro-title strong {
      font-size: 0.63rem;
      padding-left: 0.36rem;
    }

    .intro-donate {
      font-size: 0.72rem;
      padding: 0.45rem 0.58rem;
    }

    .landing-intro-hero {
      grid-template-columns: 1fr;
      min-height: 320px;
      gap: 0.65rem;
    }

    .landing-intro-quote {
      padding: 0.9rem;
      gap: 0.7rem;
      overflow: visible;
      display: flex;
    }

    .intro-steps {
      grid-template-columns: 1fr;
    }

    .intro-btn {
      font-size: 0.8rem;
      min-width: 124px;
      min-height: 40px;
      padding: 0.45rem 0.78rem;
      border-radius: 12px;
    }

    .landing-intro-image-wrap {
      aspect-ratio: 4 / 3;
      min-height: 300px;
      order: -1;
      border-radius: 14px;
    }

    .hero-image-headline {
      left: 0.72rem;
      right: auto;
      bottom: 0.72rem;
      padding: 0.72rem;
      max-width: min(88%, 360px);
      border-radius: 12px;
    }

    .hero-image-headline h2 {
      font-size: clamp(1.4rem, 6.9vw, 2rem);
      line-height: 1.02;
    }

    .hero-image-kicker {
      font-size: 0.62rem;
      padding: 0.28rem 0.5rem;
    }

    .hero-image-cta {
      font-size: 0.72rem;
      padding: 0.32rem 0.62rem;
    }

    .hero-kicker {
      font-size: 0.7rem;
      letter-spacing: 0.14em;
    }

    .landing-scholarship-copy h1 {
      font-size: clamp(1.55rem, 8.2vw, 2rem);
    }

    .landing-flyer {
      max-height: 500px;
    }

    .hero-scholarship-note {
      padding: 0.62rem 0.68rem;
      font-size: 0.95rem;
    }

    .scholarship-process {
      grid-template-columns: 1fr;
    }

    .landing-stat-row {
      grid-template-columns: 1fr;
    }

    .scholarship-study-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 390px) {
    .landing-flyer {
      max-height: 420px;
    }

    .landing-scholarship-copy h1 {
      font-size: clamp(1.45rem, 9vw, 1.75rem);
    }

    .landing-stat-card h3 {
      font-size: 1.18rem;
    }

    .scholarship-process span {
      font-size: 0.76rem;
      padding: 0.48rem 0.4rem;
    }
  }
</style>
